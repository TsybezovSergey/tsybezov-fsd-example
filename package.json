{
  "name": "my",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "dotenv -e .env concurrently -k -n \"BACK,FRONT\" -c \"blue,green\" \"npm run backend\" \"wait-on http://localhost:$BACKEND_PORT && npm run frontend\"",
    "start:backend": "npm run start:dev --prefix backend",
    "start:frontend": "npm run start --prefix frontend",
    "docker": "docker-compose up --build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "dotenv-cli": "^11.0.0",
    "wait-on": "^9.0.1"
  }
}
